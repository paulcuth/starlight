---
  machine:
    node:
      version: "iojs-v2.3.0"
    environment:
  test:
    override:
      - npm run test
  deployment:
    npm:
      branch: dev
      commands:
        - node ci/bump-project-version
        - grunt build-plugin
        - node ci/sync-plugin-version
        - npm publish dist/build-tools/grunt-starlight
        - node ci/tag